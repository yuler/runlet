<div class="mb-8">
  <%= link_to t(".back"), admin_path, class: "text-sm text-muted" %>
</div>

<h1 class="text-2xl font-bold mb-8"><%= t(".title") %></h1>

<div class="grid grid-cols-2 gap-4 mb-8">
  <div class="p-6 border rounded-xl">
    <div class="text-xs text-muted uppercase mb-2"><%= t(".accounts") %></div>
    <div class="text-3xl font-bold"><%= number_with_delimiter(@accounts_count) %></div>
  </div>
  <div class="p-6 border rounded-xl">
    <div class="text-xs text-muted uppercase mb-2"><%= t(".users") %></div>
    <div class="text-3xl font-bold"><%= number_with_delimiter(@users_count) %></div>
  </div>
</div>

<div class="mt-8">
  <h2 class="text-xl font-bold mb-4"><%= t(".personal_accounts") %></h2>
  <div class="border rounded-xl">
    <div class="grid grid-cols-3 gap-4 p-4 text-xs text-muted uppercase">
      <div><%= t(".account") %></div>
      <div class="text-right"><%= t(".users") %></div>
      <div class="text-right"><%= t(".created") %></div>
    </div>
    <% @personal_accounts.each do |account| %>
      <div class="grid grid-cols-3 gap-4 p-4 border-t">
        <div class="font-medium"><%= account[:name] %></div>
        <div class="text-right"><%= number_with_delimiter(account[:users]) %></div>
        <div class="text-right"><%= l(account[:created_at].to_date) %></div>
      </div>
    <% end %>
  </div>
</div>
