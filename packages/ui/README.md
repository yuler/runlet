# @runlet/ui

Shared UI design assets for Runlet.

## CSS

Use `src/index.css` as the package entry. It currently exposes design tokens as CSS custom properties.

```css
@import "@runlet/ui/css";
```

The source of truth for the visual direction is [`../../DESIGN.md`](../../DESIGN.md).
